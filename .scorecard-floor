# OpenSSF Scorecard floor for this repository. See ADR-054.
#
# Read by .githooks/pre-push. Every value is what this repository actually scored
# when the gate landed, so the gate can only ever ratchet upwards.
#
# Raise a number here when a check genuinely improved and you want the new value
# defended from now on. Never lower one: a check that fell is a control that broke,
# and the fix is the control. -1 means the check did not apply to this repository.
#
# aggregate is scorecard's own weighted score over the checks that scored >= 0.

aggregate=1.6

Binary-Artifacts=10
Branch-Protection=0
CI-Tests=-1
CII-Best-Practices=0
Code-Review=0
Contributors=0
Dangerous-Workflow=-1
Dependency-Update-Tool=0
Fuzzing=0
License=9
Maintained=0
Packaging=-1
Pinned-Dependencies=-1
SAST=0
Security-Policy=0
Signed-Releases=-1
Token-Permissions=-1
Vulnerabilities=1
